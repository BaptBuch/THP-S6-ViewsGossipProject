<html>
  <body class= bg-light>
    <section>
        <div class="px-4 py-5 my-5 text-center">
          <h1 class="display-5 fw-bold">Bienvenue sur The GOSSIP PROJECT</h1>
            <div class="col-lg-6 mx-auto">
              <p class="lead mb-4">Sinon, je suppose que tu es là parce que tu veux des potins ?</p>
              <p class="lead mb-4">Alors en voici en voilà !</p>
              <p class="lead mb-4">Et tu peux aussi en créer en cliquant sur le bouton ci-dessous (mais il faudra t'inscrire chez nous) :</p>
              <%= link_to 'Balance ton potin !', new_gossip_path, class:"btn btn-warning" %>  <br/>
            <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
          <p>Si tu as déjà un compte utilisateur chez nous: <%= link_to 'Se connecter', new_session_path, class:"btn btn-success" %> 
        </div>
    </section>

    <section class="container card-group">
    
      <% @gossips.each do |gossip| %> 
      
        <div class="row row-cols-1 row-cols-md-2 g-4">
          <div class="col">
            <div class="card m-3 bg-white text-dark" style="width: 18rem;">
              <img src="https://img.pngio.com/gossip-podcast-gossip-png-500_500.png" class="card-img-top" alt="...">
              <div class="card-body text-center">
                <h5 class="card-title text-muted small">Gossip numéro <%= gossip.id%></h5>
                <p class="card-text ">Son titre: <%= gossip.title%></p>
                <p class="card-text text-primary small"> Son auteur : <%= gossip.user.first_name%> </p>
                <%= link_to "Lire son contenu", gossip_path(gossip.id), class:"btn btn-primary" %>
                <%= link_to "Editer son contenu", edit_gossip_path(gossip.id), class:"btn btn-outline-info" %>
                
              </div>
            </div>
          </div>
        </div>
        
      <% end %>
    </section>

  </body>

</html>