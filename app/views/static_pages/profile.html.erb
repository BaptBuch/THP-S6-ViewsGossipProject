<% flash.each do |k, v| %>
      <div id="<%= k %>"><%= v %></div>
    <% end %>

<h2>Bienvenue <%= User.find_by(id: session[:user_id]).first_name %> !</h2>

<p>Voici ton profil:</p>

<p>
<%= User.find_by(id: session[:user_id]).first_name %><br/>
<%= User.find_by(id: session[:user_id]).last_name %><br/>
<%= User.find_by(id: session[:user_id]).email %><br/>
<%= User.find_by(id: session[:user_id]).age%><br/>
<%= User.find_by(id: session[:user_id]).description %></p>

<%= link_to "Se déconnecter", session_path(session.id), class:"btn btn-danger", method: :delete %>